package com.delivery.liveasy.model.enums;

/**
 * Enum representing the status of a booking.
 */
public enum BookingStatus {
    PENDING,   // Initial status when a booking is created
    ACCEPTED,  // Status when a booking is accepted
    REJECTED   // Status when a booking is rejected
}
